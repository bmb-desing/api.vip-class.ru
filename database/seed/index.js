const event = require('./eventsSeed');
const news = require('./newsSeed');
const objects = require('./objectsSeed');
const seed = {
	event: event,
	news: news,
	objects: objects
}
module.exports = seed